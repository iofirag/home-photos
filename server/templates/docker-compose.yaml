name: client-stack

services:

  redis:
    image: docker.io/redis:7-alpine
    container_name: immich_redis
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  database:
    image: ghcr.io/immich-app/postgres:14-vectorchord0.4.3-pgvectors0.2.0
    container_name: immich_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_DATABASE_NAME}
      POSTGRES_INITDB_ARGS: "--data-checksums"
    volumes:
      - ./client-data/immich/postgres:/var/lib/postgresql/data
    shm_size: 128mb
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d ${DB_DATABASE_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
      
  immich-server:
    image: ghcr.io/immich-app/immich-server:release
    container_name: ${IMMICH_CONTAINER_NAME}
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_HOSTNAME: ${DB_HOSTNAME}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_DATABASE_NAME: ${DB_DATABASE_NAME}
      REDIS_HOSTNAME: redis
      TZ: ${TZ}
      IMMICH_TELEMETRY_INCLUDE: all
      IMMICH_EXTERNAL_URL: https://photos-${DEVICE_ID}.${BASE_DOMAIN}
    volumes:
      - ./client-data/immich/library:/usr/src/app/upload
      # - /etc/localtime:/etc/localtime:ro
    ports:
      - "2283:2283"
    depends_on:
      redis:
        condition: service_healthy
      database:
        condition: service_healthy
    healthcheck:
      disable: false

  immichframe:
    image: ghcr.io/immichframe/immichframe:latest
    container_name: ${IMMICHFRAME_CONTAINER_NAME}
    restart: unless-stopped
    env_file:
      - .env
    # environment:
      # ImmichServerUrl: http://immich-server:2283
      # IMMICHFRAME_CONFIG_PATH: /app/Config
    volumes:
      - ./client-data/immichframe/Config:/app/Config:ro
    ports:
      - "8080:8080"
    depends_on:
      - ${IMMICH_CONTAINER_NAME}

  immichframe-config-updater:
    build: ./immichframe-config-updater
    container_name: immichframe-config-updater
    restart: unless-stopped
    environment:
      IMMICH_SERVER_URL: http://${IMMICH_CONTAINER_NAME}:${IMMICH_PORT}
      IMMICHFRAME_CONTAINER_NAME: ${IMMICHFRAME_CONTAINER_NAME}
      IMMICHFRAME_CONFIG_DIR: /data/immichframe/Config
    volumes:
      - ./client-data/immichframe/Config:/data/immichframe/Config
      - /var/run/docker.sock:/var/run/docker.sock
      # - ./templates/immichframe/config:/app/templates/immichframe/config:ro
    ports:
      - "8081:8081"
    depends_on:
      - ${IMMICH_CONTAINER_NAME}

  caddy:
    image: caddy:2-alpine
    container_name: caddy
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile
    depends_on:
      - ${IMMICH_CONTAINER_NAME}
    # ports:
    #   - "8080:80" # testing only

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    restart: unless-stopped
    command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TUNNEL_TOKEN}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - caddy

  # immich-kiosk: # Works
  #   image: ghcr.io/damongolding/immich-kiosk:latest
  #   container_name: immich-kiosk
  #   restart: unless-stopped
  #   tty: true
  #   env_file:
  #     - .env
  #   environment:
  #     KIOSK_IMMICH_API_KEY: ${KIOSK_IMMICH_API_KEY}
  #     KIOSK_IMMICH_URL: ${KIOSK_IMMICH_URL}
  #   volumes:
  #     - ./immich-kiosk/config:/config
  #   ports:
  #     - 3000:3000
  #   depends_on:
  #     redis:
  #       condition: service_healthy
  #     database:
  #       condition: service_healthy
  #     immich-server:
  #       condition: service_started
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
  #     interval: 30s
  #     timeout: 5s
  #     retries: 3
  #     start_period: 10s

  # immich-machine-learning: # Not Tested
  #   image: ghcr.io/immich-app/immich-machine-learning:${IMMICH_VERSION:-release}
  #   container_name: immich_machine_learning
  #   restart: unless-stopped
  #   env_file:
  #     - .env
  #   volumes:
  #     - ./immich/model-cache:/cache
  #   healthcheck:
  #     disable: false

